Set_v1 included with dataset which is not segmented using any techniques seperated as train val test.
Set_v2 included with dataset which segmented by K-Means clustering method and seperated as train val test.
Set_V3 included with dataset images which segmented using removebg online tool saved in train and validation files and the U-Net model results saved as test  